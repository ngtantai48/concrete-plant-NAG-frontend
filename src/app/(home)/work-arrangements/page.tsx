import WorkArrangementExperimentPage from "@/components/features/work-arrangement/WorkArrangementExperimentPage";

// Bản "thử nghiệm" (chọn từ select) đang là bản chính; bản gốc kéo-thả tạm dời về /work-arrangements/experiment.
export default function WorkArrangementsPage() {
  return <WorkArrangementExperimentPage />;
}
